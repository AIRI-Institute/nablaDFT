[pytest]
filterwarnings =
    ignore::DeprecationWarning

markers =
    dataset: datasets classes tests
    models: models tests
    pipelines: full pipeline test
    download: tests for datasets availability to download
    optimization: tests for optimization pipeline parts
